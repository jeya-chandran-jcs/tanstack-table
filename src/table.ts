// import type { DataApi } from "./types"; // adjust the path if needed

// declare module '@tanstack/react-table' {
//   interface TableMeta<TData extends unknown> {
//     onDelete?: (row: Partial<DataApi>) => void;
//     onEdit?: (row: Partial<DataApi>) => void;
//   }
// }